//Examples from FastLED
/**
 * @brief Rainbow waves
 * @param leds the array containing the CRGB values
 * @param startIndex the first index in leds to start the animation (INCLUSIVE)
 * @param endIndex the last index in leds to end the animation (EXCLUSIVE)
 */ 
void pride(CRGB* leds, uint16_t startIndex, uint16_t endIndex);

/**
 * @brief Ocean waves
 * @param leds the array containing the CRGB values
 * @param startIndex the first index in leds to start the animation (INCLUSIVE)
 * @param endIndex the last index in leds to end the animation (EXCLUSIVE)
 */ 
void pacifica(CRGB* leds, uint16_t startIndex, uint16_t endIndex);

/**
 * @brief Back and forth rainbow colors
 * @param leds the array containing the CRGB values
 * @param startIndex the first index in leds to start the animation (INCLUSIVE)
 * @param endIndex the last index in leds to end the animation (EXCLUSIVE)
 */ 
void rainbow(CRGB* leds, uint16_t startIndex, uint16_t endIndex);

/**
 * @brief Confetti effect
 * @param leds the array containing the CRGB values
 * @param startIndex the first index in leds to start the animation (INCLUSIVE)
 * @param endIndex the last index in leds to end the animation (EXCLUSIVE)
 */ 
void confetti(CRGB* leds, uint16_t startIndex, uint16_t endIndex);

/**
 * @brief Sinusoidal wave
 * @param leds the array containing the CRGB values
 * @param startIndex the first index in leds to start the animation (INCLUSIVE)
 * @param endIndex the last index in leds to end the animation (EXCLUSIVE)
 */ 
void sinelon(CRGB* leds, uint16_t startIndex, uint16_t endIndex);

/**
 * @brief Beating heart effect
 * @param leds the array containing the CRGB values
 * @param startIndex the first index in leds to start the animation (INCLUSIVE)
 * @param endIndex the last index in leds to end the animation (EXCLUSIVE)
 */ 
void bpm(CRGB* leds, uint16_t startIndex, uint16_t endIndex);

//TODO ADD MORE DESCRITIVE DESCRIPTIONS
/**
 * @brief i dont know 
 * @param leds the array containing the CRGB values
 * @param startIndex the first index in leds to start the animation (INCLUSIVE)
 * @param endIndex the last index in leds to end the animation (EXCLUSIVE)
 */ 
void juggle(CRGB* leds, uint16_t startIndex, uint16_t endIndex) ; 

void Fire2012(CRGB* leds, uint16_t startIndex, uint16_t endIndex, uint16_t cooling, uint16_t sparking);